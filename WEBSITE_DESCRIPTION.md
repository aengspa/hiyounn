# Vibe Coding Security Agent — 웹사이트 소개

## 프로젝트 개요

AI 개발 도구로 만든 웹 서비스의 보안 문제를 이해하고, 수정 전후의 결과를 확인하도록 돕는 팀 프로젝트입니다. 취약점 발견부터 근거 확인, 수정안 생성, 공격 재현과 정상 기능 재검사까지 하나의 화면 흐름으로 제공합니다.

핵심 흐름은 **보안 스캔 → 근거 확인 → 수정안 생성 → 수정 적용 → 보안 재검사 → 정상 기능 재검사 → 해결 확인**입니다. 현재 코드는 이 흐름을 체험하는 MVP이며, 실제 분석 기능과 내장 데모를 함께 사용합니다.

## 주요 화면

| 화면 | 경로 | 설명 |
|---|---|---|
| 소개 페이지 | `/` | 서비스의 목적과 보안 검증 흐름을 소개합니다. |
| 대시보드 | `/dashboard` | 등록한 프로젝트를 확인하고 새 프로젝트를 추가합니다. |
| 프로젝트 추가 | `/dashboard/new` | 이름, GitHub 저장소 주소, 배포 주소와 선택적으로 소스 코드를 입력합니다. |
| 프로젝트 상세 | `/dashboard/projects/[id]` | 프로젝트 정보와 스캔 이력을 확인하고 보안 스캔을 실행합니다. |
| 스캔 결과 | `/dashboard/scans/[id]` | 발견된 문제, 심각도, 검사 범위와 검사하지 않은 범위를 확인합니다. |
| 취약점 상세 | `/dashboard/findings/[id]` | 영향, 코드 및 요청·응답 근거, 수정안과 검증 결과를 확인합니다. |

## 보안 분석 기능

| 기능 | 현재 구현 범위 |
|---|---|
| 비밀값 노출 탐지 | 정규식으로 검사 컨텍스트의 비밀값 패턴을 탐지하고 근거에서 값을 마스킹합니다. 기본 대상은 내장 데모 파일입니다. |
| 접근 권한 / IDOR | 내장 사용자 API의 소유권 검사 누락을 탐지하고, 함수 기반 요청 재현으로 공격 및 수정 결과를 확인합니다. |
| 보안 헤더 / CORS | 입력된 배포 주소에 읽기 전용 HTTP 요청을 보내 점검합니다. 정적 설정을 사용하는 대체 검사도 있습니다. |
| 의존성 취약점 | 데모 의존성과 미리 정의된 권고 정보를 사용하는 시뮬레이션입니다. 실시간 CVE 데이터베이스 조회는 구현되지 않았습니다. |
| Supabase RLS | 데모 정책 상태를 사용하는 시뮬레이션입니다. 실제 Supabase 프로젝트 설정 조회는 구현되지 않았습니다. |
| AI 코드 분석 | LLM 설정과 붙여넣은 코드가 있으면 외부 AI API로 분석합니다. AI 분석 결과 자체가 공격 재현이나 해결 검증을 의미하지는 않습니다. |
| 수정안 생성 | 알려진 유형은 규칙 기반 수정안을 사용합니다. 그 외 유형은 LLM 설정 시 AI 생성을 시도하고 실패하면 규칙 기반으로 대체합니다. |

## 대표 시연: 다른 사용자의 정보 접근 차단

IDOR는 다른 사용자의 식별자를 지정하여 그 사람의 정보에 접근하는 권한 취약점입니다. 내장 `Acme Notes (demo)` 프로젝트에서 다음 과정을 시연할 수 있습니다.

1. 대시보드에서 데모 프로젝트를 열고 보안 스캔을 실행합니다.
2. 다른 사용자의 개인정보 접근 항목에서 요청과 응답 근거를 확인합니다.
3. 사용자 A가 사용자 B의 정보(`/api/users/102`)를 요청하면 수정 전에는 `200` 응답으로 정보가 반환됩니다.
4. 수정안을 생성하고 적용하면 상태가 `fixed`가 됩니다.
5. 검증을 실행하여 동일한 공격이 `403`으로 차단되는지 확인합니다.
6. 사용자 A의 본인 정보(`/api/users/101`) 조회 등 정상 기능이 유지되는지 검사합니다.
7. 보안 검사와 정상 기능 검사가 모두 통과해야 해당 항목이 `resolved`가 됩니다.

이 과정은 내장 데모 핸들러와 메모리 상태에서 수행됩니다. 외부 GitHub 저장소의 파일을 직접 수정하거나 커밋하는 기능은 아닙니다.

## 기술 구성

- 프론트엔드 및 서버: Next.js 14.2.15 App Router, React 18.3.1, TypeScript 5.4.5
- 스타일: Tailwind CSS 3.4.10
- API: `src/app/api`의 Route Handler
- 보안 분석: 공통 스캐너 인터페이스와 `SecurityOrchestrator`
- 데이터: 서버 메모리의 Map 기반 저장소
- 데이터베이스 확장 준비: `supabase/schema.sql`의 PostgreSQL 테이블 및 RLS 정책
- 선택적 AI 연동: OpenAI, Anthropic, Gemini 제공자 클라이언트

```text
src/
  app/          소개, 대시보드, 프로젝트·스캔·취약점 화면 및 API
  components/   탐색 메뉴, 스캔 버튼, 취약점 액션 등 UI
  lib/
    ai/         선택적 LLM API 클라이언트
    domain/     프로젝트, 취약점, 수정 및 검증 자료형
    scanners/   스캐너와 실행 조정 모듈
    demo/       취약한 예제 앱과 검사 데이터
    remediation/ 수정안 생성
    store/      메모리 저장소 및 소유권 검사
supabase/
  schema.sql    데이터베이스 확장용 스키마
```

## 로컬 실행

프로젝트 폴더에서 다음 명령을 실행합니다.

```bash
npm ci
npm run dev
```

브라우저에서 `http://localhost:3000`에 접속합니다. 기본 데모는 AI API 키 없이 실행할 수 있습니다.

선택적으로 `.env.example`을 `.env.local`로 복사한 뒤 `LLM_PROVIDER`, `LLM_API_KEY`, 필요 시 `LLM_MODEL`을 설정합니다. 붙여넣은 코드의 AI 분석은 선택한 외부 제공자로 코드를 전송하므로 실제 비밀값을 제거한 코드를 사용해야 합니다. 실제 환경변수 파일은 저장소에 포함하지 않습니다.

배포용 빌드는 다음 명령을 사용합니다.

```bash
npm run build
npm run start
```

## 현재 범위와 향후 확장

현재 인증은 고정 데모 사용자이며 실제 로그인 연동은 없습니다. 저장소 계층에는 소유권 검사가 있으나 실제 사용자 인증과는 별개입니다. 데이터는 프로세스 메모리에 저장되어 서버 재시작 시 유지되지 않습니다.

GitHub 주소를 입력해도 실제 저장소를 자동 복제하여 전체 코드를 검사하지는 않습니다. 기본 검사 컨텍스트는 내장 예제이며, 붙여넣은 코드는 선택적 AI 분석 대상으로 추가됩니다. `DATA_STORE=supabase` 설정만으로 실제 데이터베이스 저장소가 연결되지는 않습니다. AWS 연결과 배포 자동화도 현재 구현 범위에 포함되지 않습니다.

후속 개발에서는 실제 인증과 영구 저장, GitHub 코드 수집, 실제 의존성 취약점 및 RLS 검사, 외부 프로젝트의 수정 적용과 검증을 확장할 수 있습니다. 화면의 해결 표시는 검사한 개별 취약점에 대한 결과이며 웹사이트 전체의 보안을 보장하는 판정은 아닙니다.
